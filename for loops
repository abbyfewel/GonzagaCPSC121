#include <iostream>
using namespace std;

int main() {
  int x, y;
  for (x=1, y=1; x<=5; x++) {
    cout << "x is " << x << ", y is " << y << "\n";
  }
  cout << "x is " << x << ", y is " << y << "\n";
}
